name: Commands Shell

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: "Echo a string"
        run: echo "Hellow World!"
      - name: multiline script
        run: |
          node -v
          npm -v
